import 'package:flutter/material.dart';

class EnterChatRoom extends StatelessWidget {
  const EnterChatRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
